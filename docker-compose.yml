services:
  mywebsite:
    image: ghcr.io/ferilee/mywebsite:${IMAGE_TAG:-latest}
    container_name: mywebsite
    ports:
      - "127.0.0.1:4128:4128"
    volumes:
      - ${FERILEE_DATA_DIR:-./data}:/app/data
    environment:
      - NODE_ENV=production
      - PORT=4128
      - DATABASE_URL=file:/app/data/sqlite.db
    env_file:
      - .env
    restart: always
    healthcheck:
      test: ["CMD", "bun", "-e", "fetch('http://127.0.0.1:4128/healthz').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - ferileenet

networks:
  ferileenet:
    driver: bridge
